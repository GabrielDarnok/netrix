# ─── Authentication ──────────────────────────
# Generate with: openssl rand -base64 32
AUTH_SECRET="generate-a-random-secret-here"
# Optional: pre-define admin password (if omitted, npm run setup generates a random one)
# ADMIN_INITIAL_PASSWORD="your-strong-password"

# ─── Database (PostgreSQL) ───────────────────
DB_HOST="127.0.0.1"
DB_PORT="5432"
DB_NAME="flowdb"
DB_USER="pmacct"
DB_PASS="your_password_here"

# ─── Telegram Alerts (Optional) ─────────────
TELEGRAM_BOT_TOKEN=""
